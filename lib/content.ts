// eslint-disable-next-line
const UNIDICI_DOCS_URL = (branch: string) =>
  `https://raw.githubusercontent.com/nodejs/undici/${branch}/docs`;

/**
 * @TODO Add some function that fetches the content of the UNIDICI_DOCS_URL
 */
